@{
    Layout="_MainLayout";
    ViewData["HeadTitle"]="Quản lý đơn hàng";
}
@model List<QuanLyBanSach.Models.ChiTietHoaDon>
<section id="cart_items">
    <div class="table-responsive cart_info">
        <table class="table table-condensed" style="margin-bottom: auto">
            <thead>
                <tr class="cart_menu">
                    <td class="image">Mã sách</td>
                    <td>Tên sách</td>
                    <td class="">Số lượng</td>
                    <td class="">thành tiền</td>
                </tr>
            </thead>
            <tbody> 
                @if(@Model.Count() > 0)
                {
                    foreach(var item in Model)
                    {
                        <tr>
                            <td class="cart_price text-center">
                                <p><a href="~/Sach/Index/@item.Sach.id">#@item.Sach.id</a></p>
                            </td>
                            <td class="cart_price">
                                <p>@item.Sach.TenSach</p>
                            </td>
                            <td class="cart_price">
                                <p>@item.SoLuong</p>
                            </td>
                            <td class="cart_price">
                                <p>@item.ThanhTien</p>
                            </td>
                        </tr>
                    }
                }
                else
                {
                    <tr>
                        <td colspan="3" class="text-center">Bạn chưa đặt hàng lần nào</td>
                    </tr>   
                }    
            </tbody>
        </table>
    </div>
</section>